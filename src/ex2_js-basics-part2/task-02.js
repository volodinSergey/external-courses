const getArrayItemsAndLengthInConsole = (array) => {
  array.forEach((arrayItem) => console.log(arrayItem));
  console.log(array.length);

  return undefined;
};

module.exports = getArrayItemsAndLengthInConsole;
